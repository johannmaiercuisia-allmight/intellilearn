{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "start": "concurrently --names \"LARAVEL,REACT,AI\" --prefix-colors \"green,cyan,magenta\" \"php artisan serve\" \"cd frontend && npm run dev\" \"cd ai-service && venv\\\\Scripts\\\\activate && uvicorn app:app --reload --port 8001\""
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}
